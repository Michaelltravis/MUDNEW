## 2025-02-12 - [Accessible Toggles]
**Learning:** Custom "toggle" buttons using only CSS classes for state (e.g. `.active`) are invisible to screen readers.
**Action:** Always pair visual toggle states with `aria-pressed="true/false"` to ensure state is communicated programmatically.
