# graphics-02 / round 1 / builder: HUD and feed

Files: `src/web_isometric/platformer.html` (one CSS block appended last + a `#frame-vignette` div), `src/web_isometric/platformer/ui.js` (one line in `renderCompass`). `ui-arpg.js` untouched.

- Graphics-01 r3 verdict, city/dungeon: "no single focal point, six things of equal weight in the dock". BrowserQuest's strip has one loud element (the red health bar). HP is now the only saturated, large readout: 28px bar, 16px numerals, 12px bold HP label, red glow. MP/MV drop to 18px bars at 85% opacity with 12.5px numerals and no letter labels (the colour is the label; the letters collided with the numerals in a 92px cell).
- Verdict, legibility: the level/xp/gold line moved from 12.5px monospace grey to the 13px UI face (`Lv 30 Warrior` in gold 700, the rest quiet white); action labels 12.5 -> 13px on 56px slots, key numerals 12px.
- The active stance chip was a solid gold fill competing with HP; it is now an outlined chip (gold text on dark, no glow). PANELS is a 55% ghost (full on hover / open; Tab still toggles). The clock chip lost its box and sits at 42%.
- Compass: only real exits render (`.cmp:not(.on) { visibility:hidden }`), as 12px bold gold keys in the cross layout; the box moved 42px left so `D`/`E` no longer sit under PANELS. Fixed a pre-existing bug: the secret-passage marker was absolutely positioned against the whole grid (no relative parent) and drew as a stray red `?` at the compass corner; it is now a small purple `?` pinned inside its own cell (`.cmp.hidden { position: relative }`, text glyph instead of the emoji in ui.js).
- The 64px letterbox bands (inherent: the 24x15 room zoom-fits at 3.0 = 1152x720) were two flat grey slabs. The body is now a dark radial gradient, the tactical grid dropped to 16%, and a fixed `#frame-vignette` (inset shadow, z-index 7, pointer-events none) darkens the world's edges so the bands read as a painterly frame around the play area instead of dead chrome. It is not one of the rect-measured ids, so play-area insets are unchanged.
- Room pill 18px with a gold hairline and drop shadow; it remains the single top-edge focal point (the target frame replaces it in combat, unchanged).
- Feed rhythm: newest line 14.5px/20px, the two before it 13px at 70% and 12.5px at 45% (was 78%/60%), so the latest beat leads harder; incoming/outgoing pills keep their red/green edges.
- Checks: `node tools/qc_platformer_rooms.js` -> `[topdown] ALL CHECKS PASSED`. `python3 tests/test_suite.py localhost 4000 --smoke` -> `Login failed after 15 steps ... Could not login` (telnet login flow on the running server; the same failure the graphics-01 r3 builder recorded, unrelated to the client files touched). Verified visually with `tools/gauntlet/capture.js --only city,dungeon,combat` into the scratchpad.
